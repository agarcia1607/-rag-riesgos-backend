# Reproducible RAG System

Reproducible RAG with a deterministic baseline (no API keys required) and an optional LLM mode.

## Quickstart (no tokens)
```bash
pip install -r requirements.txt
uvicorn main:app --reload
